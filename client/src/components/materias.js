import React, { Component } from "react"

export default class Materias extends Component{

    render(){
        return (
            <div>
                Materias
            </div>
        )
    }
}